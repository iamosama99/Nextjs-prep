export default function InheritanceOverviewPage() {
  return (
    <div>
      <h1>Metadata Inheritance — Overview</h1>
      <p>
        This page sets no <code>metadata</code> of its own, so it inherits the root layout&apos;s{" "}
        <code>title.default</code> (&quot;Site&quot;) and full <code>description</code>/
        <code>openGraph</code>.
      </p>
    </div>
  );
}
