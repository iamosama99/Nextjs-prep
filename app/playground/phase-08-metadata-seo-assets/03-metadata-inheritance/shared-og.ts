// The docs' own fix for wanting to share some nested fields across segments while
// overriding others: pull the shared piece into a plain constant and spread it in.
export const sharedOgImage = { images: ["/og/site.png"] };
