export default function VariantA() {
  return (
    <div>
      <h1>Variant A</h1>
      <p>Bucketed into A. The URL is still <code>/ab-test</code> — this is a rewrite, not a redirect.</p>
    </div>
  );
}
