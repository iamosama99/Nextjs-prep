export default function VariantB() {
  return (
    <div>
      <h1>Variant B</h1>
      <p>Bucketed into B. The URL is still <code>/ab-test</code> — this is a rewrite, not a redirect.</p>
    </div>
  );
}
