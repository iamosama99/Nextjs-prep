export default function HelloPage() {
  return (
    <p>
      URL is <code>/playground/phase-02-app-router/04-route-groups/hello</code> — no <code>/alpha/</code>{' '}
      segment, even though the file lives in <code>(alpha)/hello/page.tsx</code>.
    </p>
  );
}
