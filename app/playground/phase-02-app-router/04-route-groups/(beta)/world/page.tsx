export default function WorldPage() {
  return (
    <p>
      URL is <code>/playground/phase-02-app-router/04-route-groups/world</code> — no <code>/beta/</code>{' '}
      segment, even though the file lives in <code>(beta)/world/page.tsx</code>.
    </p>
  );
}
